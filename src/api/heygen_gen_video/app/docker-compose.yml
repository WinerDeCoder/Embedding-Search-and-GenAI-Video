
services:
  back_end:

    build:
      context: .
      dockerfile: Dockerfile
    container_name:  HeyGen_API_test
    volumes:
      - .:/app
    ports:
      - "8006:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
